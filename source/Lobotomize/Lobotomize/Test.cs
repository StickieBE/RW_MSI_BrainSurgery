using RimWorld;
using System.Text;
using UnityEngine;
using Verse;

namespace Lobotomize
{
    public class Test : HediffComp
    {
        /*
        public override void CompPostTick(ref float severityAdjustment)
        {
            if (Pawn != null)
            {
                if (Pawn.RaceProps != null)
                {
                    if (Pawn.RaceProps.IsFlesh)
                    {
                        PlsLogMessage("OMGYES!");
                    }
                }
            }
        }

        public override string CompDebugString()
        {
            return "Testing123";
        }

        public static readonly TraitDef TheTrait = TraitDefOf.Psychopath;
        private static void PlsLogMessage(string _m)
        {

            //pawn.story.traits.GainTrait(new Trait(traitDef, traitDegreeData.degree, false));
            Log.Message("Hello man, this is the debug log working! (" + TheTrait.ToString() + ")" + _m, false);
        }
        */
    }
}
